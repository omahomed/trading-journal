# Git Quick Reference for market_school_rules.py

## ✅ You're all set up!
- Git repository initialized
- Current working version committed (with all bug fixes)

## 📝 Daily Workflow

### Before making changes:
```bash
# See what's changed
git status

# See line-by-line changes
git diff market_school_rules.py
```

### Save your changes:
```bash
# Stage the file
git add market_school_rules.py

# Commit with a message
git commit -m "Description of what you changed"
```

### Quick commit (all in one):
```bash
git commit -am "Your change description"
```

## 🔍 View History

### See all commits:
```bash
git log --oneline
```

### See detailed history with changes:
```bash
git log -p market_school_rules.py
```

### See what changed in a specific commit:
```bash
git show <commit-hash>
```

## ⏮️ Restore Previous Versions

### View file from a previous commit (don't change anything):
```bash
git show <commit-hash>:market_school_rules.py
```

### Restore to a previous version:
```bash
# Option 1: Restore and keep as uncommitted change (safer)
git checkout <commit-hash> -- market_school_rules.py

# Option 2: Revert to exact state (creates new commit)
git revert <commit-hash>

# Option 3: Go back in time (use with caution)
git reset --hard <commit-hash>
```

### Undo uncommitted changes:
```bash
# Undo all changes since last commit
git restore market_school_rules.py

# Or the old way:
git checkout -- market_school_rules.py
```

## 🏷️ Tag Important Versions

### Create a tag for a working version:
```bash
git tag -a v1.0-working -m "Verified against IBD - April 22 FTD correct"
```

### List tags:
```bash
git tag -l
```

### Restore to a tagged version:
```bash
git checkout v1.0-working
```

## 🌐 Backup to GitHub (Optional)

### Create a GitHub repository, then:
```bash
git remote add origin https://github.com/yourusername/market-school-rules.git
git branch -M main
git push -u origin main
```

### Daily push to GitHub:
```bash
git push
```

## 🆘 Emergency Recovery

### Made a mistake? See recent commits:
```bash
git reflog
```

### Restore to ANY previous state:
```bash
git reset --hard HEAD@{n}  # where n is from reflog
```

## 💡 Pro Tips

1. **Commit often** - Small, frequent commits are better than large ones
2. **Write clear messages** - Future you will thank present you
3. **Tag working versions** - Easy to find known-good states
4. **Push to GitHub** - Protects against computer crashes

---
Generated by Claude Code
