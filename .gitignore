# Streamlit secrets (contains database password)
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
/tmp/

# Local database backups
*.sql.backup

# Frontend
frontend/node_modules/
frontend/.next/
frontend/.env.local

# MCP config (local)
.mcp.json

# API secrets
api/.streamlit/secrets.toml

# CSV files (data is in cloud database)
portfolios/*/Trade_Log_*.csv
portfolios/*/Trading_Journal_*.csv
portfolios/*/Audit_Trail.csv
.env

# Phase 1b relabel runner — per-run snapshot CSVs of affected_rows go here.
# Auditable on the operator's local machine but not version-controlled.
snapshots/

# MORS local artifacts. Code + positions.json are tracked; data CSVs are
# fetched on demand by mors_backtest.ensure_data() via yfinance on first
# /api/sr8/monitor request (Railway containers re-download per cold start
# but cache for the container lifetime). Per-run report markdowns and
# Python pyc caches don't belong in git either.
mors/data/
mors/__pycache__/
mors/results/
